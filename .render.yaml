services:
  - type: web
    name: super-bot
    env: python
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    pythonVersion: 3.13.0
    plan: free
    autoDeploy: true
