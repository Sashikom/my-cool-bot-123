services:
  - type: web
    name: telegram-bot
    env: python
    plan: free
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    pythonVersion: 3.10.11
    autoDeploy: true
